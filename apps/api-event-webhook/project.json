{
  "name": "api-event-webhook",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/api-event-webhook/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "apps/api-event-webhook/dist",
        "format": ["cjs"],
        "bundle": false,
        "main": "apps/api-event-webhook/src/main.ts",
        "tsConfig": "apps/api-event-webhook/tsconfig.app.json",
        "assets": ["apps/api-event-webhook/src/assets"],
        "esbuildConfig": "tools/esbuild/prisma-cjs-fix.js",
        "sourcemap": true
      },
      "configurations": {
        "development": {},
        "production": {
          "generateLockfile": true,
          "sourcemap": false
        }
      }
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/apps/api-event-webhook/dist/package.json",
        "{workspaceRoot}/apps/api-event-webhook/dist/pnpm-lock.yaml"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": [
        "{workspaceRoot}/apps/api-event-webhook/dist/workspace_modules"
      ],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "api-event-webhook:build",
        "runBuildTargetDependencies": false,
        "runtimeArgs": ["--expose-gc"]
      },
      "configurations": {
        "development": {
          "buildTarget": "api-event-webhook:build:development"
        },
        "production": {
          "buildTarget": "api-event-webhook:build:production"
        }
      }
    },
    "docker:build": {
      "dependsOn": ["build", "prune"]
    }
  }
}
